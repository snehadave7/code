AZURE DATA FACTORY/ AZURE SYNAPSE ANALYTICS
COPY DATA

1) IF U WANT TO COPY ALL FILES IN A FOLDER -> USE Wildcard file path IN File path type INSIDE SOURCE. THEN PUT * IN THE WILDCARD FILE NAME, THIS WILL COPY ENTIRE FILES IN THE FOLDER.
2) IF U WANT TO COPY SPECIFIC FILES STARTING WITH SAME NAMES -> EXAMPLE SALES*.* ->.* MEANS ANY EXTENSION
3) IF U WANT TO COPY FILES WITH SPECFIC EXTENSTION-> *.CSV

GET META DATA CAPABILITES
1) You need to check if the file/folder exists
2) Get all files/folder from a target folder.
3) Get the column count.
4) Get the size of file
5) Get the last modified datetime of a file

SET VARIABLE
1) It allows to set a variable value and if u debug u can see the the number in output.
FOR EACH ACTIVITY
1)If you want to run in a sequence tick on sequential, if u want parallel, untick it and batch count is like if u put 2, 2 items at a time will run parallel.
COPY EACH FILE TO A NEW TABLE
1) GET METADATA(TO GET ALL THE FILES)

Pre-Copy Script-> write script that u want to run before copying data into the data.(for sql db) like truncate

COPY DATA WITH UPSERT
1) We need a key column to identify if the key column exists or not.

LOOKUP 
1) This will output the whole data present in any file/table using query/sp/ just file path like that....

EXECUTE PIPELINE: 
1)This will allow us to invoke pipelines

==============================================================DATA FLOW=================================================================================
1) It allows to do actual transformations like adding columns, doing aggregations etc.
2) Under projection detect correct datatype before writing as by default csv will take string as datatype.
3) Data preview allows to view the changes without running
4) Every dataflow must have a source and sink

1) SELECT TRANSFORMATION:
This is allow us to just select the column of a table

