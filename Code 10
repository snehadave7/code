I need to create an agent that will automatically perform everything that is mentioned in the @ruleset.md  for any type of table, data. I want to make the whole process generic so that whenever i add new table data and its schema it will perform all the dq checks and then in another file load all the failed records. After this run the data quality checks on the data to clean them up and again check if the failed records are now decreased or not. Also i have GPT-4o-mini keys so leave the space for adding the keys for llm.
